<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <link rel="stylesheet" href="./stylesheets/home.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

</head>
<body>

  <nav class="navbar navbar-expand-lg bg-customcolor">
    <div class="container-fluid">
      <a class="navbar-brand text-white" href="#">To-Do Tracker</a>
      <button class="navbar-toggler bg-white " type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active text-white" aria-current="page" href="/home">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-white" href="/addtask">Add Task</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-white" href="/logout">LogOut</a>
          </li>
        </ul>
        <span class="navbar-text text-white">
          Pen By <%=user.fullname  %>
        </span>
      </div>
    </div>
  </nav>
      </nav>

<div>
  <% if (user.tasks.length==0) { %>
    <h4 class="textarea">No Task Added !!</h4>
  <% } %>


</div>

      

      <div class="container custom-opacity">
        <div class="cards flex">
            <% user.tasks.forEach(function(post){%>
                <br>
                <div class="card " style="width: 18rem;">
                    <div class="card-body">
                        <h5 class="card-title">TASK </h5>
                        <p class="card-text"><%= post.postText %></p>
                      <ul class="list-group list-group-flush">
          



                            <% if (post.completed==true) { %>
                              <li class="list-group-tem"><%=post.currentDate%></li>
                            <% } else { %>
                              <li class="list-group-em"><%=post.currentDate%></li>
                            <% } %>
                            

                     
                        <form action="/home/<%= post._id %>"  method="POST">
                          
                          <% if (post.completed==true) { %>
                            <button class="completed" onclick="changeColor1(this)">Completed</button>
                          <% } else { %>
                            <button class="uncompleted" onclick="changeColor1(this)">Uncompleted</button>
                          <% } %>
                        </form>
                        
                        
            
                        <form action="/home/<%= post._id %>?_method=Delete" method="POST">

                          <button class="completion" onclick="changeColor(this)">Delete</button>
                        </form>
                        
                      </ul>
                      </div>
                  </div>
                <% }) %>
        </div>
     </div>

      

      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
      <script src="./javascripts/home.js"></script>
</body>
</html>